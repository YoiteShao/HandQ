{
  "meta": {
    "id": "audit",
    "description": "Parallel scan (reviewer + adversarial + critic) -> Converge -> Verdict",
    "version": 1
  },
  "entry": "scan",
  "nodes": [
    {
      "name": "scan",
      "type": "parallel_group",
      "children": ["scan_reviewer", "scan_adversarial", "scan_critic"],
      "params": {"join": "any_ok"}
    },
    {
      "name": "scan_reviewer",
      "type": "agent",
      "sub_goal": "Review for correctness issues: {goal}. Judge whether the result is correct and complete. Report concrete problems, or state clearly that it passes.",
      "tools": ["read", "grep"]
    },
    {
      "name": "scan_adversarial",
      "type": "agent",
      "sub_goal": "Adversarially audit the prior work for: {goal}. Actively hunt for false positives, missed cases, and changes that went beyond what was asked. Report every concern with evidence.",
      "tools": ["read", "grep", "glob"]
    },
    {
      "name": "scan_critic",
      "type": "agent",
      "sub_goal": "Critique whether the work actually satisfies the ORIGINAL goal: {goal}. Ignore whether intermediate steps ran; judge end-to-end intent.",
      "tools": ["read"]
    },
    {
      "name": "converge",
      "type": "convergence"
    },
    {
      "name": "verdict",
      "type": "predicate_gate",
      "params": {
        "predicate": "severity_at_least",
        "min_severity": "high"
      }
    }
  ],
  "edges": {
    "scan": {"ok": "converge", "fail": "converge"},
    "converge": {"*": "verdict"},
    "verdict": {"yes": "END", "no": "END"}
  }
}
